# 의도: modify_file (현재/기존 파일 수정)

이미 있는 파일(보통 현재 열린 화면)을 고치거나, 다른 파일의 내용을 가져와 적용/연동/교체해 달라는
요청. 슬롯(대상 파일·내용 출처)은 별도 추출한다.

- 현재 화면의 jsx 부분을 다른 파일 내용으로 적용해줘
- 이 페이지에 목록 API를 연동해줘
- 버튼 색을 파란색으로 바꿔줘
- 지금 열린 파일에 검색 필터를 추가해줘
- 테이블에 데이터를 바인딩해줘
- 현재 컴포넌트의 제목을 수정해줘
- 이 화면 레이아웃을 2단으로 변경해줘
- 입력 폼에 유효성 검사를 붙여줘
- 참조 파일의 마크업을 현재 페이지에 옮겨줘
- 날짜 입력을 달력 컴포넌트로 교체해줘
- 이 함수 리팩터링 해줘
- 불필요한 코드 제거해줘
- 현재 파일에서 useState를 useApi로 바꿔줘
- 헤더에 로그아웃 버튼 추가해줘
- 이 목록을 페이지네이션 되게 수정해줘
- apply the jsx from the reference file to this page
- bind the employee list api to the table here
- 선택한 부분 코드를 고쳐줘
- 이 페이지가 쓰는 StatusBadge 컴포넌트를 수정해줘
- 현재 화면에 상세보기 버튼을 연결해줘
- 스타일을 다크모드 대응으로 변경해줘
- 폼 제출 핸들러를 구현해줘
- 정렬 기능을 추가해줘
- 이 컴포넌트에 로딩 상태 처리 넣어줘
- 기존 코드를 새 디자인으로 교체해줘
- 입력값을 서버 파라미터로 연결해줘
- this screen's markup should be updated to use the shared layout
- 현재 파일의 import 경로를 정리해줘
- 이 페이지를 참고 컴포넌트랑 동일하게 맞춰줘
- 버튼 톤이 너무 칙칙한데 좀 밝게 손봐줘
