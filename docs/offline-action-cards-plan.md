# 오프라인 추천 카드 (Offline Action Cards) — 설계 계획

> 상태: **Phase 0 완료** (2026-08-31 — 스키마·파서·내장 카드 4장·매칭 엔진·드라이런, §9 진행표) → 다음 Phase 1
> 확정 이력: 2026-08-28 카드 파일 형식 §4 · 추천 표시 2형태 §3.5 · 계획 카드 §3.6 / 2026-08-31 슬롯 소스 v1 §4.5
> 작성: 2026-08-28
> 도식: `docs/diagrams/09-오프라인-추천카드.svg`
> 관련 메모리: [offline-mode-enhancement], [offline-intent-responder], [intent-routing-collision-safety], [region-disambiguation], [recipe-contract-cards], [region-failure-capture]

---

## 1. 배경과 문제의식

Axiom의 오프라인 모드는 "LLM을 쓸 수 없는 환경(폐쇄망·서버 미기동)에서도 프론트엔드
개발자에게 최대한 실질적인 도움을 준다"는 목적으로 설계됐다. 다른 AI 어시스턴트가
전부 LLM 연결을 전제하는 것과 다른, Axiom 고유의 차별점이다.

그런데 현재 오프라인 구조에는 구조적 한계가 하나 있다:

- **자연어 → 의도 분류(정규식/IntentSignals) → 바로 실행** 구조는 오분류가 곧
  사고가 된다 (modify→create 누수로 기존 파일 덮어쓰기 사례).
- 모델 없는 자연어 이해는 "프롬프트마다 엣지버그 무한"의 두더지잡기다
  ([intent-routing-collision-safety]에서 확정된 진단).
- 그래서 오프라인의 "행동(생성·수정)"을 자연어 실행에 그대로 맡기는 방향은
  구조적으로 정직하지 않다.

### 핵심 통찰 — 결정론 층은 이미 모델 없이 돈다

Axiom이 약한 sLLM을 보정하려고 쌓아온 층들 — locate, structural apply,
ScaffoldContracts 계약/레시피 카드, ComponentPropsIndex, 페이지 템플릿,
봉투계약 탐지기 — 은 **애초에 모델 없이 동작하는 코드**다. 파이프라인에서 모델이
실제로 맡는 건 ① 자연어 이해 ② 작은 판단 한 조각(필드 매핑, 후보 중 선택)뿐인
경우가 많다 (compose binding: "분해·대조는 결정론, 모델은 필드매핑 한 조각").

**→ 오프라인 모드 = 그 "모델의 한 조각"을 사람의 클릭(추천 카드·위저드·객관식)으로
치환한 도구 모음.** region disambiguation에서 "우선순위는 미리 알 수 없다 →
이해하는 쪽에 위임"이라 결론냈는데, 오프라인에서는 그 '이해하는 쪽'이 모델이 아니라
사용자 본인이 된다.

---

## 2. 핵심 설계 결정 (확정)

1. **자연어의 역할을 "실행"에서 "추천"으로 바꾼다.**
   입력창은 그대로 두되, 오프라인에서 행동성 요청은 바로 실행하지 않고
   **버튼이 달린 추천 카드**를 렌더하는 것까지만 자연어(정규식)가 담당한다.
   실행은 버튼 클릭 → QuickPick 위저드(결정론)가 이어받는다.
   - 오분류가 무해해진다: 틀려도 "엉뚱한 버튼이 하나 보일 뿐", 사용자가 다른
     버튼을 누르면 끝. 정규식 두더지잡기가 "추천 품질"이라는 저위험 문제로 격하.
   - 채팅 입력창이 "명령 실행기"에서 "검색창"으로 바뀌는 것 (유튜브 검색 비유:
     검색어를 쳐도 영상이 바로 재생되지 않고 결과 목록이 뜬다).

2. **Q&A와 행동을 분리한다.**
   - Q&A(지식 검색)는 오답 비용이 0이므로 **지금처럼 자연어 → 바로 답변** 유지
     (OfflineKnowledgeRetriever 그대로).
   - 행동(생성·수정)만 추천 카드를 거친다.

3. **행동 카탈로그는 코드가 아니라 데이터(카드)다.**
   트리거·슬롯·행동을 선언하는 카드 파일로 정의하고, 엔진(매칭→정렬→위저드→적용)은
   카드 내용을 모른다. ScaffoldContracts 카드·knowledge/_index 키워드·page-templates가
   이미 같은 "트리거 → 내용물" 패턴 — 이를 하나의 카드 스키마로 승격·통합하는 것.

4. **엔진은 프레임워크 중립, 지원은 React scaffold 전용 유지.**
   Vue/Next 지원을 지금 목표로 잡지 않는다 (Axiom의 경쟁력 = scaffold 계약에 깊이
   묶인 것, 특허 차별성 서사와 동일). 단, 엔진에 React 가정을 하드코딩하지 않아
   미래에 "카드 팩 추가"만으로 확장 가능한 구조를 공짜로 얻는다.

5. **자연어를 건너뛰는 입구를 함께 제공한다.**
   채팅창 `/` 자동완성(슬래시 커맨드), 런처 패널 고정 메뉴, VSCode 명령 팔레트 —
   전부 같은 위저드로 수렴. 숙련자는 이 길로 다닌다.

6. **추천 표시는 2형태 — A(채팅 카드)가 기반, B(입력창 위 리스트)가 최종형.** (2026-08-28 확정)
   - **형태 A** = Enter 후 응답으로 뜨는 채팅 메시지 카드. B가 생겨도 없앨 수 없다:
     이미 전송된 메시지의 회수처이자, 기록이 남고, 완료 카드→"다음 단계" 버튼 연쇄가
     대화 흐름 안에서만 성립하기 때문.
   - **형태 B** = 타이핑 중 입력창 위에 실시간으로 뜨는 자동완성형 리스트
     ("입력창=검색창"의 문자 그대로의 구현). 방향키+Enter로 위저드 직행.
   - 같은 매칭 엔진, 두 출구. 구현 순서 = A 먼저(기존 채팅 카드 포맷·"Enter→응답"
     흐름을 그대로 탐), B는 그 엔진 위에 표시 컴포넌트만 얹는 후속. §3.5 참조.

7. **카드는 "메뉴"가 아니라 "계획 카드(plan card)"다.** (2026-08-28 확정)
   행동 이름 목록("~위저드 [열기]") 대신 **결과 미리보기**(파일 diff·코드 골격·
   매핑 테이블) + **프리필 칩**(위저드 = 순차 질문이 아니라 칩 편집기) + **매칭 근거
   하이라이트**로 렌더. 확신도(top1–top2 점수 격차)에 따라 계획 카드 1장 ↔ 컴팩트
   리스트로 부피 적응. 잘 맞으면 클릭 1번(Enter)으로 실행 — 정확성(실행 전 확인)과
   편의성을 동시에 확보. 상세 §3.6.

---

## 3. UI 흐름 (4장면)

### 장면 1 — 입력은 지금과 동일
```
┌─ Axiom Chat ────────────────────────────────┐
│  나: 직원 목록 페이지 만들어줘               │
└─────────────────────────────────────────────┘
```

### 장면 2 — 실행 대신 "계획 카드" (여기가 바뀌는 지점)
기존 채팅 카드 포맷(banner/출처칩)과 같은 계열의 메시지 하나. 확신 높은 매칭이면
버튼 목록이 아니라 **프리필로 채워진 작업 계획**을 렌더한다 (원칙 = §3.6):
```
┌─ Axiom Chat ──────────────────────────────────────┐
│  Axiom:                                           │
│  ┌─────────────────────────────────────────────┐  │
│  │ 📄 목록 페이지 생성        ("목록·페이지"에서) │  │
│  │                                             │  │
│  │  도메인 [main ▾]   이름 [EmployeeList ✎]     │  │
│  │  유형 [목록 list ▾]                          │  │
│  │                                             │  │
│  │  만들어질 것:                                │  │
│  │  + src/domains/main/pages/EmployeeList.tsx  │  │
│  │  ± src/domains/main/router/index.tsx (경로)  │  │
│  │  ▸ 코드 미리보기                    (접힘)   │  │
│  │                                             │  │
│  │  [⏎ 이대로 만들기]          [다른 작업 ▾]    │  │
│  └─────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```
잘 맞춘 경우 **클릭 1번(Enter)**으로 장면 4 직행. 정규식 분류 결과는 **카드 선정 +
칩 프리필**에만 반영 — 틀려도 칩을 고치거나 [다른 작업 ▾]을 열면 그만.

### 장면 2′ — 매칭이 애매하면 컴팩트 리스트 (확신도 게이트, §3.6)
```
┌─ Axiom Chat ────────────────────────────────┐
│  Axiom: ⚡ 오프라인 모드 — 이렇게 도와드릴 수 있어요: │
│  ┌─────────────────────────────────────┐    │
│  │ 📄 목록 페이지 생성 위저드    [열기] │    │
│  ├─────────────────────────────────────┤    │
│  │ 🔌 API → 테이블 바인딩 위저드 [열기] │    │
│  ├─────────────────────────────────────┤    │
│  │ 📚 관련 문서: list-page 가이드 [보기]│    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```
선택하면 그 카드가 장면 2의 계획 카드로 펼쳐진다.

### 장면 3 — QuickPick은 "칩을 클릭했을 때만" (위저드 = 순차 질문이 아니라 칩 편집기)
```
[도메인 main ▾] 칩 클릭 →  ┌──────────────────┐
                           │ 도메인 선택        │
                           │ ▸ main            │
                           │   example         │
                           │   + 새 도메인      │
                           └──────────────────┘
```
- 프리필이 전부 맞으면 **이 장면은 통째로 생략**된다 (장면 2에서 Enter → 장면 4).
- 순차 3단 QuickPick(도메인→유형→이름)은 자연어 없이 **맨땅 진입**(`/` 메뉴·런처·
  팔레트, 프리필 재료가 없는 경우)했을 때만 쓴다.
- 이 시점부터 자연어 해석은 0. page.template.txt + router.template.txt 결정론 생성.

### 장면 4 — 완료 카드 + 다음 행동 연결
```
┌─ Axiom Chat ────────────────────────────────┐
│  Axiom: ✅ 생성 완료                         │
│  · src/domains/main/pages/EmployeeList.tsx  │
│  · router.tsx 에 경로 배선됨                 │
│  다음 단계: [🔌 이 테이블에 API 바인딩] [📚 SmartTable 문서] │
└─────────────────────────────────────────────┘
```

### 3.5 추천 표시 2형태 (확정 2026-08-28)

장면 2(형태 A, 채팅 카드)는 **Enter 후**의 표시다. 최종형에서는 **Enter 전**,
타이핑 중에 입력창 위로 실시간 리스트(형태 B)가 함께 뜬다:

```
        ┌───────────────────────────────────────┐
        │ 📄 목록 페이지 생성 위저드      ↵ 선택 │
        │ 🔌 API → 테이블 바인딩 위저드          │
        │ 📚 list-page 가이드 문서               │
        ├───────────────────────────────────────┤
        │ Enter = 그냥 메시지로 보내기            │
        └───────────────────────────────────────┘
┌─ 입력 ─────────────────────────────────────────┐
│ 직원 목록 페이지▌                               │
└────────────────────────────────────────────────┘
```

| | 형태 B (입력창 위 리스트) | 형태 A (채팅 카드) |
|---|---|---|
| 시점 | Enter 전, 타이핑 중 | Enter 후, 응답으로 |
| 역할 | 빠른 진입·발견 (방향키+Enter 위저드 직행) | 놓친 경우 회수·기록·다음 단계 연쇄 |
| 구현 | 후속 (엔진 위에 표시 컴포넌트) | **기반 (Phase 1)** |

- 채팅 webview 입력창(`InputBar`)이 자체 React 컴포넌트라 B는 완전히 우리 통제하에
  구현 가능. `/` 슬래시 메뉴와 **같은 표시 컴포넌트를 공유**한다.
- 리스트를 무시하고 Enter로 전송하면 → 형태 A 카드로 회수 (두 형태는 대체가 아니라 보완).
- 매칭은 트리거 키워드 필터 수준이라 타이핑 중 실시간(디바운스) 부담 없음.
- 형태 A의 내용물 = §3.6 계획 카드(확신 높음) 또는 컴팩트 리스트(애매).

### 3.6 계획 카드 (plan card) — 카드는 "메뉴"가 아니라 "채워진 작업 계획" (확정 2026-08-28)

행동의 이름("~위저드 [열기]")은 해석을 요구한다. 개발자가 해석 없이 즉시 읽는 건
**파일 경로·diff·코드**다. 그래서 형태 A 카드의 내용물을 버튼 목록이 아니라
"이미 절반 완성된 작업 계획"으로 렌더한다.

**원칙 3가지**
1. **행동 이름이 아니라 결과를 보여줘라.** 템플릿 카드 = 만들 파일 경로(diff 트리),
   레시피 카드 = 삽입될 코드 골격, API 바인딩 = 매핑 테이블. 전부 카드가 이미 아는
   결정론 정보라 추가 엔진 비용 0.
2. **묻지 말고 채워서 보여줘라.** 프리필로 일단 다 채운 계획 + 슬롯은 인라인 칩.
   위저드가 "순차 질문"에서 "틀린 칩만 고치기"로 바뀐다 → 잘 맞으면 클릭 1번(Enter),
   하나 틀리면 2번. "몇 번 더 클릭" 문제는 위저드 구조의 산물이었지 정확성의 대가가
   아니었다 — 이 구조로 정확성(실행 전 확인)과 편의성을 동시에 가진다.
3. **근거를 보여줘라.** 매칭된 트리거를 사용자 문장에 하이라이트("직원 **목록**
   **페이지** 만들어줘") — 시스템 추론이 보이면 신뢰가 생기고, 오매칭 시 원인이
   즉시 보인다 (카드 작성자의 트리거 디버깅에도 기여).

**확신도 적응형 부피** — top1–top2 매칭 점수 격차 게이트
- **확신 높음** → 계획 카드 1장(장면 2), Enter 즉시 실행. 체감은 "알아듣고 준비해놨네"
  지만 안전 속성(실행 전 계획 가시화 + 명시적 확인)은 유지 — **미리보기 자체가
  사람 눈으로 하는 검증 게이트**("추가 LLM 호출 없는 검증" 서사와 일치).
- **애매함** → 컴팩트 리스트 2~3행(장면 2′), 선택하면 계획 카드로 펼쳐짐.
- 기존 버튼 리스트는 [다른 작업 ▾] 뒤로 강등, QuickPick은 칩 클릭 시의 부품으로.
  개발자가 느끼는 스펙트럼이 "명령 → 메뉴"가 아니라 "**명령 → 계획 확인**"이 되고,
  메뉴는 시스템이 확신 없을 때만 등장하는 예외 상태가 된다.

**카드 유형별 미리보기**

| 카드 유형 | 카드 본문 = |
|---|---|
| template | 파일 트리 diff (+ 신규 / ± 수정) |
| recipe | 삽입될 골격 첫 몇 줄 + 슬롯 하이라이트, 전체는 접힘 |
| API 바인딩 | **매핑 테이블 자체가 카드** — 애매한 행만 드롭다운 (위저드의 표면화, 창 전환 없음) |
| doc | 문서 첫 단락 발췌 (기존 오프라인 Q&A 카드와 동일) |

**비용·클러터**
- 미리보기는 전부 결정론(§4 스키마가 이미 선언: template=출력 경로, recipe=본문 골격)
  — 새 비용 = webview 카드 리치화(칩·접기·diff 행)뿐. 채팅 UI가 자체 React라 통제 가능.
- 클러터 방지: 코드 미리보기 기본 접힘 + 카드 높이 상한.

---

## 4. 행동 카드 파일 형식 (확정 2026-08-28)

**형식 = md + YAML frontmatter, 한 파일 = 카드 한 장** (`.axiom/actions/*.card.md`).
knowledge 문서 컨벤션과 일치하고, **frontmatter = 기계용 메타데이터 / 본문 = 사람용
설명 + 코드 골격**이라는 역할 분담이 자연스럽다. (json 단독안은 골격 코드를 문자열로
이스케이프해야 해서 기각.)

### 완전한 예시 — 프로젝트 카드 한 장

````markdown
---
schemaVersion: 1                  # 스키마 진화 대비
id: project-search-form           # 파일명과 일치 강제 (충돌 방지)
title: 표준 검색폼 삽입
icon: 🔍
triggers: [검색폼, 검색 조건, 조회 조건, search form]
preconditions: [file-open, scaffold-detected]
slots:
  - name: formName
    label: 폼 이름
    source: text                  # text | domain-list | endpoint-list | component-list
    prefillFrom: query
action:
  type: recipe                    # template | recipe | doc | command
priority: 20
---

## 설명 (패널·카드에 표시)
우리 프로젝트 표준 검색폼. Select+Input+조회버튼 골격.

## 골격 (recipe 타입일 때 — 슬롯 치환 후 structural apply로 삽입)
```tsx
const [{{formName}}Params, set{{formName}}Params] = useState<T{{formName}}Params>({});
...
```
````

### 형식 규칙 3가지 (계약)

1. **스키마 검증은 로드 시점, fail-open.** 필수 필드 누락·모르는 `action.type`·
   트리거 중복이 있으면 **그 카드만 비활성** + 패널에 ⚠ 표시(VSCode Problems 병행).
   잘못된 카드 하나가 카탈로그 전체를 죽이면 안 된다.
2. **`source` 어휘는 초기 4~5개로 한정.** `text` / `domain-list` / `endpoint-list` /
   `component-list` 수준. 슬롯 소스 = "위저드가 자동으로 채워줄 수 있는 것"의 목록이라
   여길 좁게 시작해야 카드 작성 난이도가 낮다. 확장은 스키마 버전과 함께.
3. **패널은 카드 파일을 건드리지 않는다.** 켜기/끄기 토글 상태는 카드 파일이 아니라
   **워크스페이스 설정(비활성 id 목록)**에 저장 — 파일은 git에서 항상 깨끗하게,
   "내가 껐다"는 개인 상태가 팀에 커밋되지 않게.

### 매칭 엔진

- `action.type` 종류: `template`(파일 생성), `recipe`(structural 삽입 —
  기존 레시피 카드 실행), `doc`(지식문서 렌더), `command`(위저드/명령 호출).
- 질문 토큰 vs triggers — **유한 카탈로그에 대한 검색 문제**이므로 기존 오프라인
  검색 교훈(복합키워드·공백변형 정밀화, [offline-retrieval-ranking])을 그대로 적용.
  상위 N개(2~3장)만 노출.
- 한글 트리거 주의: 정규식 `\b`는 한글에 안 먹음 ([scaffold-contract-coverage] 함정).
- 카드는 **자기 미리보기를 스스로 제공**한다: template=출력 경로들, recipe=본문 골격,
  doc=첫 단락. 계획 카드(§3.6) 렌더에 추가 스키마 필드 불필요
  (부족해지면 v2에서 `preview` 오버라이드 필드 고려).

### 4.5 슬롯 소스 v1 + 구현 계약 (확정 2026-08-31, Phase 0 ①)

규칙 2의 "정확한 목록과 스캔 계약"을 다음 5개로 확정 (`src/ai/actions/types.ts`가 진실원):

| source | 스캔 계약 | 프리필 |
|---|---|---|
| `text` | 스캔 없음 — 자유 입력(InputBox) | `prefillFrom: query` 시 결정론 추출기 |
| `enum` | 스캔 없음 — 카드가 `options:`로 고정 선택지 선언 (예: 페이지 유형 list/form) | 〃 |
| `domain-list` | `src/domains/*` 하위 디렉터리명 (+위저드가 "새 도메인" 항목 추가) | 〃 (extractDomainFromQuery 재사용) |
| `endpoint-list` | api-spec 문서에서 추출한 API 경로(extractApiPaths 계열) | 〃 |
| `component-list` | ComponentPropsIndex 컴포넌트명(자동생성 인덱스) | 〃 |

- 스캐너는 `ISlotSourceProviders`로 주입 — 엔진·파서는 vscode/디스크 비의존(테스트=스텁).
- **구현 확정 사항** (Phase 0 ①·② 코드에 반영):
  - frontmatter 파싱 = **의존성 0의 YAML 부분집합 파서**(`MiniYaml.ts`). js-yaml 미도입 —
    카드 어휘가 좁아(규칙 2) 부분집합으로 충분, 지원 밖 문법은 errors로 명시 보고.
  - `action.outputs`는 문자열 배열 `"+ 경로"` / `"± 경로 (설명)"` — §3.6 미리보기의
    `+ 신규 / ± 수정` 표기를 스키마에 그대로 사용 (중첩 객체 리스트 회피).
  - 카드 lint의 씨앗: `{{플레이스홀더}}` ↔ 슬롯 선언 교차검증(선언 없는 플레이스홀더=error,
    recipe의 미사용 슬롯=warning), 카드 내 중복 트리거 dedupe+warning.
  - 트리거 충돌 정책(`findTriggerCollisions`): **같은 계층** 중복=나중 카드 비활성(error) /
    **계층 간** 공유=warning만 — 프로젝트 카드가 내장 카드를 의도적으로 덮는 경우가
    정상이므로(§5), 정렬에서 상위 계층 가점으로 해소.
  - `preconditions` v1 어휘 = `file-open` | `scaffold-detected` (모르는 값=error).
  - `schemaVersion` 누락=1로 간주, 1 이외=그 카드만 비활성(전방 호환).

---

## 5. 카탈로그 3계층 + 관리 UI

| 계층 | 위치 | 관리 주체 | 용도 |
|---|---|---|---|
| 내장 카드 | 익스텐션 번들 | Axiom | scaffold 핵심 (페이지 위저드·API 바인딩·레시피) |
| **프로젝트 카드** | `.axiom/actions/` — **git 커밋으로 팀 공유** | 프로젝트 리더/선임 | 업무별 표준 레시피 ("우리 프로젝트 검색폼 카드") |
| 개인 카드 | globalStorage | 개인 | 개인 취향 스니펫 |

폐쇄망 SI 팀에서 프로젝트 계층이 특히 강력: 선임이 카드를 등록해 커밋하면 팀 전체가
같은 추천을 받는다. `.axiom/knowledge` 핫리로드(_AUTHORING 워크플로우)와 같은
메커니즘을 `.axiom/actions/`로 확장.

### 관리 UI 원칙 — "편집은 파일, 관리는 패널"
사용자가 개발자이므로 처음부터 폼 편집기를 만들지 않는다.
- **편집** = 카드 파일 직접 작성 (`.card.md`, §4 형식, 저장 즉시 핫리로드)
- **관리 패널** 기능:
  - 카드 목록 (계층별·활성 여부·검증 ⚠ 표시)
  - 켜기/끄기 토글 (삭제 대신 비활성화 — 상태는 워크스페이스 설정에, §4 규칙 3)
  - "새 카드 만들기" (템플릿 스캐폴딩)
  - 카드 lint (트리거 중복·슬롯 오류 검사)
  - **드라이런**: "이 카드가 어떤 질문에 뜨는지" 미리보기 — 트리거 겹침으로
    엉뚱한 카드가 위로 오는 문제(오프라인 top-N 오염과 같은 부류)를 등록 시점에 차단

---

## 6. 카드 채집 플라이휠 (등록 자동화)

등록을 전부 수작업에 두지 않는다: **온라인 모드에서 LLM이 성공적으로 해낸 편집**
(applied + 미되돌림)을 감지하면 "이 패턴을 오프라인 레시피 카드로 저장할까요?"
제안. RegionCaptureRecorder 포집 인프라(요청·적용 결과 JSONL)가 원료를 이미 보유.

→ 카탈로그가 팀의 실제 업무를 따라 자라고, **온라인 사용이 오프라인 자산을 만드는**
플라이휠. "LLM 없는 환경 대비"라는 Axiom 설계 철학의 대표 서사가 될 수 있음
(특허/발표 서사에도 얹기 좋은 조각).

---

## 7. 오프라인 기능 후보 전체 카탈로그

추천 카드가 가리킬 목적지들. ★ = 우선순위 상위 4개.

### A. 생성 — 자연어 대신 위저드
| # | 기능 | 내용 | 재사용 자산 |
|---|---|---|---|
| ★A1 | **API → 화면 바인딩 위저드** | 스펙/샘플 JSON → T타입 → `useApi<{data:T[]}>` 봉투계약 훅 → SmartTable/폼 바인딩. 모델이 하던 필드 매핑만 **매핑 테이블 UI**(정확·유사 이름 자동 프리필, 애매한 것만 드롭다운) | compose binding Stage1~3, detectEnvelopeKey, JsonTypeGenerator |
| ★A2 | **페이지/도메인 생성 위저드** | QuickPick 3단(도메인→유형→이름), 라우터 자동 배선 | page/router.template.txt, page-templates/*.md, FileCreatorService |
| A3 | 레시피 카드 실행기 | 계약카드(date-picker 등)를 명령으로 — 슬롯 입력 → 정확 골격 삽입 | ScaffoldContracts, structural apply |
| A4 | 컴포넌트 삽입기 | 컴포넌트 선택 → 필수 prop 폼 → import 포함 JSX 생성 | ComponentPropsIndex(53종), locate/앵커 |

### B. 지식·탐색 — 제자리에서 보여주기
| # | 기능 | 내용 | 재사용 자산 |
|---|---|---|---|
| ★B1 | **컴포넌트 카탈로그 패널** | props 표+예제 브라우징·검색·스니펫 복사 (데이터는 전부 있고 UI만 없음) | ComponentPropsIndex, knowledge/components |
| B2 | Hover/자동완성 승격 | `useApi` 위에 hover → 봉투계약 카드. "질문할 줄 알아야 도움받는" 채팅 한계 돌파 | ScaffoldContracts, knowledge |
| B3 | 디자인 토큰 브라우저 | tokens/*.css 파싱 → 색 견본·CSS 변수 자동완성·복사 | — (신규, 소형) |
| B4 | 라우터 맵 | domains/*/router 파싱 → 경로 트리, 고아 페이지·중복 경로 탐지 | — (신규, 소형) |

### C. 검사·교정 — 기존 게이트를 Diagnostics로 노출
| # | 기능 | 내용 | 재사용 자산 |
|---|---|---|---|
| ★C1 | **Scaffold 린트** | 모듈스코프 훅, 봉투 언랩 누락(`data?.data`), I/T 접두사, refetch 무인자, import 규칙 → VSCode Diagnostics + Quick Fix. ESLint가 못 잡는 프로젝트 고유 계약. "추가 LLM 호출 없는 검증" 특허 서사와 일치 | 편집 파이프라인 거부 게이트 탐지기들(react-rules 등) |
| C2 | 결정론 Quick Fix | import hoist, 엔드포인트 별칭 정규화, 정적배열→API 교체 채널을 code action으로 | 기존 변환기들 |

### D. 워크플로우 — 폐쇄망 팀 현실
| # | 기능 | 내용 | 재사용 자산 |
|---|---|---|---|
| D1 | publishing→domains 포팅 도우미 | 퍼블리셔 산출물 이관 결정론 보조 | OfflineTransplant, reconcileImportsWithReference |
| D2 | Mock 데이터 생성기 | T타입 → fixture JSON (백엔드 미완성 상태 개발) | JsonTypeGenerator 역방향 |
| D3 | 오프라인 큐잉 | LLM 필요한 요청을 로컬 JSONL에 기록 → 온라인 복귀 시 일괄 처리 제안 | RegionCaptureRecorder 인프라 |

**우선순위 근거**: A1·C1은 온라인 모드에서도 그대로 가치가 있어(모델 결과 검증·프리필)
오프라인 전용 투자 부담이 없음. 넷 다 새 엔진이 아니라 기존 결정론 층에 UI를 붙이는 작업.

---

## 8. 기존 자산 재사용 맵

| 기존 모듈 | 새 역할 |
|---|---|
| `ai/intent/OfflineIntentResolver`·`IntentSignals` | 실행기 → **추천기** (카드 정렬·프리필만 담당, 실행 권한 회수) |
| `ai/retrieval/OfflineKnowledgeRetriever` | Q&A 직행 경로 (변경 없음) + 카드의 `doc` 액션 렌더러 |
| `ai/contracts/ScaffoldContracts` 레시피 카드 | 카드 스키마의 `recipe` 액션으로 승격 |
| `ai/templates/*.template.txt` + `pipeline/FileCreatorService` | 카드 `template` 액션 실행기 |
| `ai/apply/` structural apply·앵커 | 위저드 결과의 결정론 삽입 |
| `ai/contracts/ComponentPropsIndex` | 카탈로그 패널(B1)·컴포넌트 삽입기(A4) 데이터 |
| `ai/JsonTypeGenerator` | A1 위저드의 타입 생성 단계 |
| `ai/pipeline/RegionCaptureRecorder` | 카드 채집 플라이휠(§6)·오프라인 큐잉(D3) 원료 |
| region disambiguation(모델 객관식) | 같은 되묻기 패턴 — 응답자만 모델→사용자 |
| `.axiom/knowledge` 핫리로드(_AUTHORING) | `.axiom/actions/` 핫리로드로 확장 |

---

## 9. 단계별 실행 계획 (초안)

- **Phase 0 — 카드 스키마 + 매칭 엔진 (기반)**
  카드 타입 정의, 내장 카드 3~4장(수기), 트리거 매칭·정렬 + **확신도 게이트**
  (top1–top2 격차, §3.6). 테스트: 카드 드라이런 하니스(질문 → 뜨는 카드 top-N +
  계획카드/리스트 모드 스냅샷, eval:region 계기판과 동형 철학).
  - 진행 (2026-08-31, `src/ai/actions/`): ① 스키마 타입+슬롯 소스 계약(§4.5) ✅ ·
    ② MiniYaml+CardParser(fail-open 검증) ✅ · ③ 내장 카드 4장(`media/action-cards/`:
    create-page[template]·api-table-binding[command]·use-api-doc[doc]·insert-date-picker[recipe]) ✅ ·
    ④ CardMatcher(압축 포함 매칭·가중치=압축길이·계층 가점·확신도 게이트 기본 0.5)+
    CardCatalog(디렉터리 로드·충돌 정책) ✅ · ⑤ 드라이런(`test:action-cards` 97/0 +
    `npm run dryrun:cards -- "질문"` CLI) ✅ — **Phase 0 완료**. 다음 = Phase 1
    (계획 카드 UI 형태 A + 페이지 위저드 A2, 오프라인 직접 실행 경로를 카드 뒤로 이동)
- **Phase 1 — 계획 카드 UI(형태 A) + 페이지 위저드 (A2)**
  오프라인 행동성 요청 → 계획 카드/컴팩트 리스트 렌더(§3.6, 프리필 칩·파일 diff
  미리보기·근거 하이라이트) → 칩 편집(단일 슬롯 QuickPick) → 템플릿 생성.
  기존 오프라인 실행 경로는 카드 뒤로 이동(직접 실행 제거). 맨땅 진입용 순차
  QuickPick 병행. 코드 미리보기 접힘·높이 상한 포함.
- **Phase 2 — API 바인딩 위저드 (A1)**
  compose binding 오프라인 변형: 필드 매핑 테이블 UI.
- **Phase 3 — 카탈로그 3계층 + 관리 패널**
  `.axiom/actions/` 핫리로드, 목록/토글/새 카드/lint/드라이런 패널.
- **Phase 4 — 확장**
  입력창 위 실시간 추천 리스트(형태 B, §3.5 — `/` 슬래시 메뉴와 표시 컴포넌트 공유),
  Scaffold 린트(C1, 독립 트랙으로 병행 가능), 카탈로그 패널(B1), 채집 플라이휠(§6).

각 Phase 완료 기준: 기존 테스트 무회귀 + 해당 기능 전용 테스트 green
(오프라인 개선 시 공유 어휘스코어러 buildContext 절대 수정 금지 원칙 유지).

---

## 10. 리스크·미결정 사항

1. **온라인 모드와의 UX 이원화**: 온라인=자연어 실행, 오프라인=카드 경유의 이질감.
   → 완화: 카드 패턴이 검증되면 온라인의 "실행은 충돌 안전(되묻기)" 처방에 역수입
   가능 (오프라인이 선행 실험장).
2. **카드 매칭 품질**: 유한 카탈로그 검색이라 자연어 실행보다 훨씬 쉽지만,
   트리거 겹침·한글 토큰화는 여전한 주의 지점. → 드라이런·lint로 등록 시점 차단.
3. ~~카드 스키마 형식~~ → **해소 (2026-08-28)**: md + YAML frontmatter 확정, §4.
4. ~~슬롯 소스 표준화~~ → **해소 (2026-08-31)**: v1 = `text`/`enum`/`domain-list`/
   `endpoint-list`/`component-list` 5종 + 스캔 계약·주입 인터페이스 확정, §4.5.
5. **채집 플라이휠의 일반화 품질**: 성공 편집 → 카드 변환 시 "그 파일 전용"이 아닌
   재사용 가능한 골격으로 추상화하는 규칙 필요 (수동 확인 단계를 거치는 반자동이 안전).
6. **계획 카드의 확신도 임계**: 격차 게이트가 과신하면 엉뚱한 계획이 큰 카드로
   그려져 역효과. 임계값은 Phase 0 드라이런 하니스로 튜닝하되, 초기엔 보수적으로
   (애매하면 리스트 쪽) 시작.
